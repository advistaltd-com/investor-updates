[build]
  functions = "netlify/functions"

# Redirect all routes to index.html for client-side routing (SPA)
# Note: Netlify Functions (/.netlify/functions/*) are automatically excluded from redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
